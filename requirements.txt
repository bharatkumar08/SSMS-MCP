# ============================================================
# SQL AI Agent — Requirements
# Compatible with Python 3.14 on Windows (x64)
# No Rust/C compiler required — all packages have pre-built wheels.
#
# INSTALL ORDER (run these two commands):
#   python -m pip install --upgrade pip
#   pip install -r requirements.txt
# ============================================================

# ── Web framework (pure-Python wheels) ───────────────────────
flask>=3.1.3
flask-cors>=4.0.1

# ── OpenAI v2 SDK (pure-Python) ───────────────────────────────
openai>=2.0.0

# ── SQL Server driver ─────────────────────────────────────────
# pyodbc 5.3.0+ has a cp314 Windows wheel.
# Also requires ODBC Driver 17 or 18 on the OS:
# https://learn.microsoft.com/sql/connect/odbc/download-odbc-driver-for-sql-server
# pyodbc>=5.3.0
# Add this:
pymssql>=2.3.0
sqlalchemy>=2.0.36

# ── Data / charts ─────────────────────────────────────────────
# pandas 2.3.0+ has a cp314 Windows wheel
pandas>=2.3.0
plotly>=5.24.1

# ── MCP HTTP sidecar (pure-Python) ────────────────────────────
uvicorn>=0.44.0
starlette>=1.0.0

# ── HTTP client (pure-Python) ────────────────────────────────
httpx>=0.28.0

# ── Utilities (pure-Python) ──────────────────────────────────
python-dotenv>=1.0.1
anyio>=4.13.0
